server.servlet.context-path=
server.port=${PORT:8080}

#Это автоматически настроит HiddenHttpMethodFilter.
#Затем используйте th:method=DELETE в форме, чтобы Thymeleaf автоматически добавлял скрытое поле.
#(Spring Boot < 2.2 всегда регистрирует фильтр, для Spring Boot 2.2 или выше вам нужно установить это свойство)
spring.mvc.hiddenmethod.filter.enabled=true

#db
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

#spring.datasource.url=${JDBC_DATABASE_URL:jdbc:mysql://localhost:3306/note2?allowPublicKeyRetrieval=true&useSSL=false&useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC}
#spring.datasource.username=${JDBC_DATABASE_USERNAME:0000}
#spring.datasource.password=${JDBC_DATABASE_PASSWORD:0000}
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=validate

#spring.liquibase.change-log=classpath:db/changelog/db.changelog-master.xml


#log
logging.level.root=INFO
logging.level.org.springframework.security=INFO
